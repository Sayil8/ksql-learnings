-- Use the run script command to include files in ksql command line
    RUN SCRIPT = './ksql-example-file-name'

-- Always start from the beginning of the table/stream
  SET 'auto.offset.reset'='earliest';